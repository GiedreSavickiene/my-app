import {useState} from "react"

function Create({add}){



    

    return(
         <div>
        
    <button type='button' onClick={add}>ADD</button>
    
        </div>

    )

}
export default Create;